export default function Main(){
    return(
        <main>
            <form className="add-ingridient-form">
                <input type="text" aria-label="Add ingridient" placeholder="e.g oregano" />
                <button>+ Add ingridient</button>
            </form>
        </main>
    )
}